server {
  listen 80;
  server_name splurge.dev
  location / {
    proxy_pass http://localhost:8102/;
  }
}

server {
        listen   8102;
        
#        listen   [::]:8101;
#        listen   443 ssl;
#        listen   [::]:443 ipv6only=on ssl;
#  ssl on;
#  ssl_certificate /ssl/libdev.crt;
#  ssl_certificate_key /ssl/libdev.key;

        root /libdev/www/splurge;

        location = /favicon.ico {
                log_not_found off;
                access_log off;
        }

}
